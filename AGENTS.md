# EBS 배리어프리 — 다문화가정·외국인 언어 지원 PoC

기존 EBS 학습 영상에 검증된 AI API를 얹어, 다문화가정·외국인이 **모국어로 학습**할 수 있는 접근성 데모를 만든다. 실서비스가 아닌 **프로토타입(PoC)** 이며, 6대 기능의 작동 검증이 목표다.

---

## 핵심 설계 원칙 (반드시 준수)

이 4대 경량화 원칙이 전체 아키텍처를 결정한다. 코드를 작성할 때 항상 이 원칙에 부합하는지 점검한다.

1. **실시간 처리 대신 전처리** — 자막·번역·어휘 풀이는 빌드 타임에 미리 생성해 JSON으로 저장한다. 런타임은 JSON을 불러올 뿐, AI API를 거의 호출하지 않는다.
2. **벡터 DB 대신 메모리 계산** — 콘텐츠가 소수이므로 별도 검색 인프라 없이 메모리에서 유사도를 계산하거나 단순 매칭으로 검색한다.
3. **TTS는 추상화하되 OpenAI 기본** — TTS는 `lib/tts.js`로 추상화하되, 품질을 위해 OpenAI TTS(`/api/tts`)를 기본 엔진으로 쓴다(브라우저 TTS는 폴백). 초기 브라우저 TTS는 검증용이었고 OpenAI TTS로 교체 완료.
4. **소수 콘텐츠로 6기능 검증** — 주제가 다른 시범 콘텐츠 **영상 1 + 텍스트 1**로 6대 기능을 모두 시연한다 (영상=①~⑥, 텍스트=②③④⑤). 언어 확장은 코드가 아니라 데이터(JSON) 추가만으로 이뤄지도록 설계한다.
   > **텍스트 축 갈음(2026-08-11 결정)**: 별도 텍스트 콘텐츠 타입(`type: "text"`)은 구현하지 않고, **영상 콘텐츠 메타데이터(제목·설명·해시태그·강사명)의 10개 언어 번역으로 갈음**한다. ② 텍스트 번역 검증이라는 원래 목적은 메타데이터 번역으로 이미 달성됐다고 판단. 스키마는 문서에 남겨 두되 미구현 상태다(남은 과업 참고).

---

## 기술 스택

| 레이어 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | React + Vite | |
| 스타일 | Tailwind CSS | 접근성 UI 우선 |
| 서버(런타임) | Vercel 서버리스 함수 (`/api`, Node.js) | API 키 보관·런타임 호출용. 상시 서버 대신 서버리스 |
| AI 호출 | Claude API + OpenAI(Whisper·임베딩·TTS) | 전처리는 로컬, 런타임 키 호출은 `/api` |
| 영상·자막 | Plyr (또는 Video.js) | 다국어 자막 트랙 지원 |
| 데이터 | JSON 파일 | DB 없이 시작 |
| 배포 | GitHub → Vercel | repo 푸시 시 자동 빌드·배포 (온담 키오스크와 동일) |

---

## 지원 언어

- 지원 대상(10개): 원본 한국어(ko) + **영어(en) · 베트남어(vi) · 중국어(zh) · 일본어(ja)**
  - 다문화가정: **태국어(th) · 러시아어(ru) · 인도네시아어(id)**
  - 글로벌: **스페인어(es) · 프랑스어(fr)**
- 언어는 **코드가 아니라 데이터로** 관리한다. 추가 언어는 `languages` 배열에 코드를 넣고 해당 언어 JSON만 생성하면 확장된다.
- 안정화 이후 언어를 추가할 예정이므로, 언어 코드를 하드코딩하지 말 것.

```json
"languages": ["ko", "en", "vi", "zh", "ja", "th", "ru", "id", "es", "fr"]
```

> **NOTE**: 이 언어 확장은 실서비스가 아니라 **PoC의 기술 검증**이다. 목적은 "언어를 코드 수정 없이 **데이터(JSON) 추가만으로 확장**할 수 있다"는 아키텍처의 확장성을 입증하는 것이며, 각 언어의 번역·음성은 **AI(Claude·OpenAI) 생성 결과 수준**으로 **원어민 검수를 통한 실서비스 품질을 보증하지는 않는다**. 실서비스화 시 언어별 원어민 검수가 별도로 필요하다.

---

## 폴더 구조

```
ebs-barrierfree/
├── AGENTS.md
├── .env                  # API 키 (로컬 전용, gitignore 필수)
├── .gitignore            # .env, node_modules, dist, source/ 제외
├── vercel.json           # SPA 폴백 rewrite (/api 제외한 모든 경로 → index.html)
├── package.json
├── design/               # 디자인 레퍼런스 (커밋 — 에이전트가 참고)
│   └── references/
│       ├── list_page.png            # 리스트 페이지 레퍼런스
│       ├── content_page.png         # 상세 페이지 레퍼런스
│       ├── style_guide_web.png      # EBS 배리어프리 스타일 가이드 (웹)
│       └── style_guide_mobile.png   # EBS 배리어프리 스타일 가이드 (모바일)
├── source/               # 원본 954MB MP4 (로컬 전용, 커밋 금지 — STT 입력용)
│   └── sample-01.mp4
├── public/               # 정적 자산 (영상 제외 — 영상은 EBS CDN URL로 재생)
│   └── dub/{contentId}/{lang}/  # ⑥ 사전 생성 더빙 mp3 ({segmentId}.mp3)
├── scripts/              # 전처리 (로컬에서 1회 실행, Vercel에선 실행 안 함)
│   ├── transcribe.js     # Whisper STT → 한국어 자막+타임코드
│   ├── translate.js      # Claude 교정 + 자막·본문·UI 4개 언어 번역
│   ├── translate-images.js # Claude Vision: 이미지 텍스트+bbox 추출·번역 (②)
│   ├── vocabulary.js     # 어휘 풀이 사전 생성
│   ├── build-content.js  # 콘텐츠 메타(content.json) 다국어 생성
│   ├── build-search-index.js # ④ 콘텐츠 임베딩 인덱스 생성
│   ├── extend-langs.js   # 신규 언어 증분 확장(기존 값 보존 — 메타·자막·어휘·이미지·UI)
│   └── dub.js            # ⑥ 자막→OpenAI TTS 더빙 mp3 + duration 메타 생성
├── data/                 # 전처리 산출물 (커밋해서 배포에 포함)
│   ├── ui-strings.json   # 전역 UI 문자열 사전 (②)
│   ├── search-index.json # ④ 콘텐츠 임베딩 인덱스
│   └── {contentId}/      # 콘텐츠별 (영상 type=video / 텍스트 type=text)
│       ├── content.json      # 제목·설명·해시태그·(텍스트면 body) 다국어
│       ├── subtitles.json    # 영상만
│       ├── vocabulary.json
│       ├── images.json       # 이미지 텍스트+bbox 번역 (②)
│       └── dub-{lang}.json   # ⑥ 더빙 메타 (언어별 mp3 경로 + duration)
├── api/                  # Vercel 서버리스 함수 (API 키 보관·런타임 호출)
│   ├── search.js         # ④ 질의 임베딩 + 유사도 (키 서버사이드)
│   └── tts.js            # ③ OpenAI TTS 폴백 (선택)
└── src/                  # React + Vite 프론트
    ├── components/
    │   ├── VideoPlayer.jsx
    │   ├── DualSubtitle.jsx        # ① 이중자막 (보여주기 전용)
    │   ├── Transcript.jsx          # 대본 패널 — ⑤ 어휘 · ③ 읽어주기 · 구간 이동
    │   ├── TranslatableImage.jsx   # ② 이미지 번역 오버레이
    │   ├── VocabularyTooltip.jsx   # ⑤ 어휘 즉시 풀이
    │   ├── ReadAloud.jsx           # ③ 읽어주기 버튼 (텍스트 옆)
    │   ├── DubbingControl.jsx      # ⑥ 자막 음성 더빙
    │   └── SemanticSearch.jsx      # ④ 의미 기반 검색
    ├── lib/
    │   ├── i18n.js                 # t(key, lang) — UI 문자열 사전 (②)
    │   ├── audioBus.js             # 오디오 매니저 — 한 번에 한 목소리 (③⑥+원음)
    │   ├── tts.js                  # TTS 추상화 (브라우저↔OpenAI 교체 가능)
    │   └── similarity.js           # 메모리 코사인 유사도
    └── App.jsx
```

---

## 데이터 구조 (JSON 스키마)

전처리 산출물의 형태. 런타임은 이 형태만 신뢰한다.

**content.json** — 콘텐츠 메타. `type`으로 영상/텍스트 구분. 텍스트·해시태그·설명은 모두 다국어(②-텍스트).
```json
{
  "id": "ebs-sample-01",
  "type": "video",
  "title":       { "ko": "...", "en": "...", "vi": "...", "zh": "...", "ja": "..." },
  "description": { "ko": "...", "en": "...", "vi": "...", "zh": "...", "ja": "..." },
  "hashtags":    [ { "ko": "감정", "en": "emotion", "vi": "cảm xúc", "zh": "情感", "ja": "感情" } ],
  "videoUrl": "https://ebsvod.ebs.co.kr/ebsvod/cul/2024/40049780/2m/20241007_094000_1f712df8_m20.mp4",
  "languages": ["ko", "en", "vi", "zh", "ja"]
}
```

**subtitles.json** — 세그먼트 단위. 모든 언어를 한 세그먼트에 담아 이중자막·더빙이 동일 타임코드를 공유한다.
```json
{
  "segments": [
    {
      "id": 0,
      "start": 0.0,
      "end": 3.2,
      "text": { "ko": "...", "en": "...", "vi": "...", "zh": "...", "ja": "..." }
    }
  ]
}
```

**vocabulary.json** — 주요 단어 사전. 툴팁 표시용.
```json
{
  "terms": [
    {
      "term": "광합성",
      "segmentId": 4,
      "def": { "ko": "...", "en": "...", "vi": "...", "zh": "...", "ja": "..." }
    }
  ]
}
```

**ui-strings.json** — 전역 UI 문자열 사전(②). `data/ui-strings.json`. 메뉴·버튼·라벨 등.
```json
{
  "nav.search":     { "ko": "검색", "en": "Search", "vi": "Tìm kiếm", "zh": "搜索", "ja": "検索" },
  "player.dubbing": { "ko": "더빙", "en": "Dubbing", "vi": "Lồng tiếng", "zh": "配音", "ja": "吹き替え" }
}
```
> `t('nav.search', lang)`으로 꺼내 쓴다.

> `content.json`의 `title`·`description`·`hashtags`는 모두 다국어 객체다(②-텍스트). 위 기본형 참고.

**images.json** — 이미지 속 한국어 번역(②-이미지). `data/{contentId}/images.json`.
```json
{
  "images": [
    {
      "id": "thumb-01",
      "src": "https://.../thumb.jpg",
      "regions": [
        {
          "bbox": [0.06, 0.70, 0.50, 0.20],
          "text": { "ko": "1강 감정이 주는 힘", "en": "...", "vi": "...", "zh": "...", "ja": "..." }
        }
      ]
    }
  ]
}
```
> 전처리 때 Claude Vision으로 이미지 속 텍스트+위치를 추출. `bbox`는 0~1 **정규화 좌표**(이미지 크기 무관하게 오버레이). 런타임은 원본 위에 번역 텍스트를 덮을 뿐 **재합성하지 않는다.**

**② 번역 레이어 원칙 (한번에 코딩의 핵심)**
- 모든 텍스트는 `t(key, lang)`(UI) 또는 콘텐츠의 다국어 필드를 거친다 — **한국어 하드코딩 금지.**
- 모든 콘텐츠 이미지는 `<TranslatableImage>`로 감싼다 — 번역 표시 모드면 `regions` 오버레이.
- 텍스트·이미지 번역은 **전역 '번역 표시' 모드 하나**가 함께 제어한다.

**content.json (텍스트 콘텐츠)** — `type`으로 영상/텍스트 구분. 텍스트는 `body`(문단 단위 다국어).
```json
{
  "id": "ebs-text-01",
  "type": "text",
  "title": { "ko": "...", "en": "...", "vi": "...", "zh": "...", "ja": "..." },
  "body": [
    { "id": 0, "text": { "ko": "...", "en": "...", "vi": "...", "zh": "...", "ja": "..." } }
  ],
  "languages": ["ko", "en", "vi", "zh", "ja"]
}
```
> 영상은 `type: "video"` + `videoUrl`·`subtitles.json`, 텍스트는 `type: "text"` + `body`. 텍스트 학습 페이지는 영상 상세와 **같은 번역·읽어주기·어휘 레이어**를 쓰되 플레이어·더빙만 없다. 텍스트=②③④⑤, 영상=①~⑥ → 둘이 합쳐 6기능 전부.
> **단, `type: "text"`는 스코프 아웃(2026-08-11)** — 위 스키마는 설계 기록으로만 남기고 구현하지 않는다. ② 텍스트 번역은 영상 메타데이터의 10개 언어 번역으로 검증됐다.

**search-index.json** — ④ 의미 검색용. `data/search-index.json`. 전처리에서 콘텐츠 임베딩을 미리 계산해 저장.
```json
{
  "model": "text-embedding-3-small",
  "items": [
    { "contentId": "ebs-sample-01", "type": "video", "title": "1강 감정이 주는 힘", "embedding": [0.012, -0.034, "..."] }
  ]
}
```
> 런타임: 사용자 질의만 `/api/search`에서 임베딩 → 이 배열과 메모리 코사인 유사도. 임베딩이 부담되면 키워드 매칭 폴백.

**dub-{lang}.json** — ⑥ 더빙 메타. `data/{contentId}/dub-{lang}.json`(예: `dub-vi.json`). `scripts/dub.js`가 `subtitles.json`의 해당 언어 자막을 OpenAI TTS로 음성 생성 → `public/dub/{contentId}/{lang}/{segmentId}.mp3`에 저장하고, 세그먼트별 메타를 이 파일에 기록한다.
```json
{
  "contentId": "ebs-sample-01",
  "lang": "vi",
  "model": "tts-1",
  "voice": "alloy",
  "count": 42,
  "segments": [
    { "id": 0, "start": 0.0, "end": 3.2, "duration": 2.7, "audio": "/dub/ebs-sample-01/vi/0.mp3" }
  ]
}
```
> `duration`은 **실제 생성된 음성 길이**(초). 자막 구간 길이는 `end - start`. 런타임은 세그먼트마다 `duration`을 구간 길이와 비교해 **음성이 짧으면 남는 시간을 침묵으로 두고, 길면 영상을 일시정지했다가 음성이 끝나면 재개**한다(⑥ 길이 맞춤). `audio`는 `public/` 기준 정적 경로라 런타임 TTS 호출 없이 mp3만 재생한다.

---

## 전처리 파이프라인 (scripts/ — 오프라인 실행)

시범 영상에 **공식 자막이 없으므로 STT부터 시작**한다. 빌드 타임 1회 실행이며 런타임과 분리한다. **원본이 954MB라 Whisper 업로드 한도(25MB)를 크게 초과하므로 오디오 추출·분할이 필수다.**

1. **오디오 추출** — `transcribe.js`: ffmpeg로 MP4에서 음성만 뽑아 16kHz 모노 mp3(약 64kbps)로 압축. 영상 대비 용량이 수십 분의 1로 줄어든다.
2. **분할(필요 시)** — 압축 후에도 25MB를 넘으면 무음 구간 기준으로 24MB 이하 청크로 나눈다.
3. **STT** — 각 청크를 **`whisper-1`** 모델로 전사(`response_format: verbose_json`으로 세그먼트 타임코드 확보). 청크별 **시작 시각 오프셋**을 더해 타임코드를 원본 기준으로 보정·병합한다. (타임스탬프가 필요하므로 gpt-4o-transcribe가 아닌 whisper-1을 쓴다.)
4. **교정** — `translate.js`: Claude로 STT 오류·구두점·청크 경계 보정(한국어 기준 텍스트 확정).
5. **번역** — `translate.js`: Claude로 세그먼트별 4개 언어 번역 → `subtitles.json`.
6. **어휘** — `vocabulary.js`: Claude로 주요 단어 추출 + 4개 언어 풀이 → `vocabulary.json`.
7. 산출물을 `data/{contentId}/`에 저장. 결과는 **관리자 검수**를 거쳐 품질을 보정한다(수동 수정 가능한 형태 유지).

> ffmpeg가 로컬에 설치돼 있어야 한다. STT 입력용 원본 MP4는 커밋하지 않고 로컬 `source/`에만 둔다.

---

## 6대 기능 명세 (구현 순서대로)

| 순서 | 기능 | 엔진 | 구현 메모 |
|---|---|---|---|
| 1 | ① 동영상 이중자막 | 전처리 결과(JSON) | 한국어 + 선택 모국어를 영상 위에 동시 표시. 다른 기능의 기준이 되므로 가장 먼저 |
| 2 | ② 텍스트·이미지 번역 | Claude / Claude Vision | 이미지는 재합성하지 말고 **추출 후 오버레이**로 번역 표시 |
| 3 | ⑤ 어휘 즉시 풀이 | 전처리 사전(JSON) | 자막 단어 클릭/호버 시 툴팁. 사전 생성된 풀이만 사용 |
| 4 | ③ 음성 안내(TTS) | 브라우저 TTS → OpenAI TTS | 모국어·한국어 양방향. `lib/tts.js`로 추상화해 교체 가능하게 |
| 5 | ⑥ 자막 음성 더빙 | 사전 생성 mp3 + 자막 타임코드 | 더빙 음성은 실시간이 아니라 `scripts/dub.js`로 **사전 생성**(언어별 mp3 + duration 메타). 런타임은 **①의 타임코드**에 맞춰 재생하되, 음성이 자막 구간보다 **짧으면 침묵 허용 / 길면 영상 일시정지 후 재개**로 길이 맞춤. audioBus 우선순위(읽어주기 > 더빙 > 원음)·원음 더킹 20% 유지 |
| 6 | ④ 의미 기반 검색 | 임베딩 / Claude + 메모리 유사도 | 모국어 질의 → 의미 통하는 한국어 콘텐츠 검색. 콘텐츠 메타가 다 갖춰진 뒤 |

> **검색(④) 런타임 흐름**: 콘텐츠 임베딩은 전처리에서 미리 계산해 `data/search-index.json`에 저장. 사용자 질의만 런타임에 임베딩(`/api/search` 경유) → 메모리 코사인 유사도. 임베딩이 부담되면 단순 키워드 매칭으로 폴백.

---

## API 흐름 & 보안

- **빌드 타임(로컬)**: Whisper·Claude 호출은 `scripts/`에서만, 내 컴퓨터에서 1회 실행한다. **Vercel에서는 전처리를 돌리지 않는다.** 산출물 JSON만 커밋해 배포에 포함한다.
- **런타임(배포)**: 대부분 정적 JSON 로드. API 키가 필요한 호출(검색 질의 임베딩, OpenAI TTS)은 **반드시 `/api` 서버리스 함수 경유** — 프론트엔드에 키를 노출하지 않는다.
- API 키는 두 곳에 둔다: 로컬 전처리는 `.env`(gitignore, 커밋 금지), 배포 런타임은 **Vercel 대시보드의 Environment Variables**. 코드에 키를 하드코딩하지 않는다.
- AI 생성 결과(자막·번역)는 관리자 검수 단계를 둔다(수동 보정 가능한 JSON 유지).

---

## 배포 (GitHub → Vercel)

온담 키오스크와 동일한 방식으로 GitHub 저장소 → Vercel 자동 배포를 구성한다.

1. **저장소 초기화 & 푸시** — GitHub에 새 repo(예: `ebs-barrierfree`)를 만든 뒤:
```bash
git init
git add .
git commit -m "init: EBS 배리어프리 PoC"
git branch -M main
git remote add origin https://github.com/<계정>/ebs-barrierfree.git
git push -u origin main
```
2. **`.gitignore`** — 아래는 반드시 제외(나머지는 커밋):
```
.env
node_modules
dist
source/
```
3. **커밋 대상** — `data/*.json`(전처리 산출물)과 `design/`(레퍼런스)은 **커밋한다.** 배포된 앱이 JSON을 정적으로 읽기 때문이다. `.env`·원본 키·`source/`의 954MB MP4는 절대 커밋하지 않는다.
4. **Vercel 연동** — Vercel에서 그 GitHub repo를 Import. 프레임워크 프리셋 **Vite**, 빌드 명령 `npm run build`, 출력 디렉터리 `dist`. `/api` 폴더는 Vercel이 서버리스 함수로 자동 인식한다.
5. **환경 변수** — Vercel 프로젝트 Settings → Environment Variables에 `ANTHROPIC_API_KEY`, `OPENAI_API_KEY` 등록. `/api` 함수에서만 읽힌다.
6. **자동 배포** — 이후 `main`에 푸시할 때마다 Vercel이 자동 빌드·배포(`git push` = 배포). PR마다 프리뷰 URL도 생성된다.

**시범 영상(MP4) 배치 — EBS CDN URL 직접 사용.** 시범 영상은 EBS VOD CDN에 이미 공개된 직접 `.mp4` 주소가 있으므로 별도 호스팅 없이 그대로 쓴다. `content.json`의 `videoUrl`이 이 CDN URL을 가리키게 하고, Plyr/HTML5 `<video>`로 재생한다. 자막은 영상 호스트의 `<track>`이 아니라 로컬 JSON 오버레이로 표시하므로 영상에는 CORS가 필요 없다(다른 출처 재생은 localhost·배포 모두 확인됨).

- **검증 완료**: 직접 미디어 URL, 만료 토큰 없음, 공개 접근, 외부 출처(localhost) 재생 OK → vercel.app에서도 재생됨.
- **주의(통제권)**: 이 URL은 EBS가 관리하므로 경로가 바뀌면 재생이 깨질 수 있다. 그 경우의 **폴백은 Cloudflare R2**다(원본 mp4를 R2 버킷에 rclone으로 1회 업로드 → 공개 URL을 `videoUrl`에 교체. R2는 10GB 무료·전송비 0·range 지원). 폴백은 깨졌을 때만 구성하면 된다.
- **STT는 무관**: Whisper는 URL이 아닌 로컬 파일을 입력으로 받으므로, 원본 mp4(또는 이 URL에서 1회 내려받은 파일)를 `source/`에 두고 전처리한다.

---

## 디자인 레퍼런스

**EBS 배리어프리 사이트 자체의 디자인 톤**을 정의한다. 익숙한 학습 서비스의 화면 문법(리스트→상세)을 따르므로, 사용자는 새 사용법을 배울 필요 없이 기존 감각 그대로 6기능을 쓸 수 있다.

- 레퍼런스 스크린샷 2장을 `design/references/`에 둔다. 화면을 만들거나 디자인할 때 **이 두 장을 먼저 참고**한다.
  - `list_page.png` — **리스트 페이지 레퍼런스** (히어로·카테고리·카드 그리드·정렬·페이지네이션·푸터)
  - `content_page.png` — **상세 페이지 레퍼런스** (영상 플레이어·번역 고지·시리즈·강사 카드·추천·댓글)
  - `style_guide_web.png` / `style_guide_mobile.png` — **EBS 배리어프리 스타일 가이드** (색·폰트·타입 스케일). 아래 디자인 토큰의 출처.
- 6기능은 새 페이지를 만들지 않고 이 두 페이지의 **자연스러운 자리에 얹는다** (상세 배치는 아래 UX 섹션).

---

## 디자인 토큰 (EBS 배리어프리 스타일 가이드)

출처: `design/references/style_guide_web.png` · `style_guide_mobile.png`.

**색 (공식 8색)**

| 역할 | hex | 쓰임 |
|---|---|---|
| Background | `#BFDCFA` | 페이지·히어로 배경 (연한 하늘색) |
| Blue | `#64A9E6` | 주요 블루 (배너·버튼) |
| Green | `#3DB653` | 카테고리·주요 액션 |
| Yellow | `#FFB629` | 강조·하이라이트 |
| Deep blue | `#21649C` | 활성 상태(선택 탭 등) + AI 기능 활성 강조 |
| Black | `#212121` | 본문 텍스트 |
| Logo navy | `#000A6B` | 로고·제목 |
| Logo green | `#00B58A` | 로고 |

> AI 접근성 기능의 활성 상태(번역 표시·더빙·읽어주기·검색 배지)는 **Deep blue `#21649C`** 로 통일해 콘텐츠 색과 구분한다. 카드/히어로의 연한 배경은 위 색의 옅은 틴트로 파생한다(공식 팔레트에 별도 민트·퍼플 없음 — 썸네일의 보라색은 콘텐츠 이미지일 뿐 브랜드색 아님).

**글꼴 (공식)** — ⚠️ 앞서 적었던 Pretendard가 아니라 가이드 기준으로 교정.
- 제목: **Gmarket Sans** (Title)
- 본문·UI: **Noto Sans CJK** — 한국어·중국어·일본어 커버. 베트남어(라틴+성조)는 같은 Noto 패밀리의 Noto Sans(라틴)로 보강해 일관되게.

**타입 스케일** (웹 / 모바일)

| 용도 | 웨이트 | 웹 | 모바일 |
|---|---|---|---|
| 제목 (Gmarket Title) | Medium | 32px | 32px |
| 메뉴 (MENU) | Medium | 20px | 27px |
| 탭 (TAB) | Regular | 16px | 24px |
| 리스트 (LIST) | DemiLight | 16px | 24px |
| 본문 버블 (Bubble) | Regular | 20px | 28px |

**간격·모서리·접근성**
- 모서리: 카드 12px, 컨트롤·버튼 8px, 토글/언어는 pill. 둥글고 친근하게.
- 여백 넉넉히. **기본 화면은 단순하게, 세부 컨트롤은 'AI 도구' 시트로** (점진적 노출).
- 탭 영역 최소 44px. 저시력 배려로 자막·본문은 모바일 타입 스케일(24~28px)을 적극 활용.

---

## UX/UI — 페이지 구성 & 컨트롤 배치

기존 리스트·상세 두 페이지 구조 위에 6기능을 얹는다. 새 페이지를 만들지 않는다.

### 전역 (모든 페이지)
- **상단 바**: `번역 표시`(한국어만 / 함께 / 모국어만) · 의미 검색(④) · `내 언어`(지구본) · `AI 도구`. **'번역 표시'(자주 바꾸는 보기 방식)와 '내 언어'(거의 안 바꾸는 정체성)는 분리**한다.
- **번역 표시 모드 하나가 텍스트(②)와 이미지(②) 번역을 함께 제어**한다(별도 토글 없음).
- 오디오는 **단일 채널 "한 번에 한 목소리"**(아래 오디오 컨트롤러 섹션).

### 리스트 페이지 (영상나눔)
히어로 → 카테고리 버튼 → 카드 그리드 → 정렬·페이지네이션 (기존 리스트 페이지 구조 그대로).
- 카드 = 썸네일 + 제목·해시태그. 번역 표시가 켜지면 **썸네일은 이미지 오버레이 번역(②), 제목·해시태그는 한국어+모국어(②)**.

### 콘텐츠 상세 페이지 (영상) — 6기능이 모이는 핵심
- **영상 플레이어**: 이중자막①은 **보여주기 전용**(클릭 타깃 없음). 플레이어 바 = 재생 · 자막 설정① · **더빙⑥** · 전체화면. (기존 '음성안내' 버튼 제거 — 영상 바엔 영상 컨트롤만.) 더빙 재생 시 "더빙 중" 표시.
- **대본 패널**(영상 옆, 모바일은 아래): 단어 클릭 = 어휘 풀이⑤ · 줄 끝 스피커 = 읽어주기③ · 줄 클릭 = 그 구간 이동. 현재 구간 강조·자동 추적. **상호작용은 모두 여기서**(작은 자막 대신 큰 타깃).
- **번역 고지**: 기존 번역 고지 문구 재사용.
- **시리즈·추천 썸네일**: 이미지 오버레이 번역(②).
- **강사 카드**: 설명·해시태그 텍스트 번역(②) + 읽어주기③ 버튼.
- **댓글**: 기존 댓글 구조 유지.

### 컨트롤 위치 요약

| 위치 | 들어가는 컨트롤 |
|---|---|
| 상단 바 | 번역 표시(②) · 검색(④) · 내 언어 · AI 도구 |
| 영상 바 | 재생 · 자막 설정(①) · 더빙(⑥) · 전체화면 |
| 대본 패널 | 어휘(⑤) · 읽어주기(③) · 구간 이동 |
| 텍스트 옆 | 읽어주기(③) |
| 카드·썸네일 | 이미지 번역(②, 번역 표시 모드 연동) |

### 반응형 · 점진적 노출
- 모바일: 대본은 영상 아래로 내려가고, 세부 컨트롤(자막 언어·크기, 더빙 언어·원음 볼륨, 읽기 속도)은 **'AI 도구' 시트**로 접는다.
- 기본 화면은 단순하게 — 디지털 친숙도가 낮은 사용자도 헤매지 않게.

---

## 오디오 컨트롤러 (한 번에 한 목소리)

세 소리(읽어주기③ · 더빙⑥ · 영상 원음)가 한 채널을 공유한다. 동시에 둘이 나지 않도록 **단일 오디오 매니저(`lib/audioBus.js`)를 모든 재생이 거치게** 한다. 컴포넌트가 각자 소리를 내면 충돌하므로, ③·⑥·⑤ 발음·원음이 전부 이 한 곳을 통한다.

**우선순위** (위가 재생되면 아래는 멈추거나 줄어듦)
1. 읽어주기 (③ / ⑤ 발음) — 사용자가 직접 누름, 최우선
2. 더빙 (⑥) — 영상 따라 자동
3. 영상 원음 — 기본 배경

**동작 규칙**
- 읽어주기 시작 → 영상(원음·더빙) 일시정지 → 읽기 끝나면 자동 복귀.
- 더빙 켜짐 → 원음 더킹(낮춤, 기본 20%).
- 새 음성 시작 → 이전 음성 즉시 취소(연타해도 안 겹침).
- 현재 무엇이 재생 중인지·왜 멈췄는지 **시각 신호**로 표시(예: "더빙 중", "읽어주기 중 · 영상 일시정지").

**인터페이스 (개념)**
- `audioBus.play({ source, priority, lang, onEnd })` — 우선순위에 따라 기존 음성을 정지/더킹하고 재생.
- `audioBus.stop(source)` · `audioBus.duckOriginal(pct)`.
- TTS 엔진(브라우저↔OpenAI)은 `lib/tts.js` 뒤에 두고 audioBus가 호출한다.

---

## 접근성 규칙 (PoC라도 준수)

- 스크린리더 호환을 전제로 시맨틱 마크업·ARIA 속성을 사용한다.
- `axe-core`로 WCAG 기본 항목을 자동 검사한다.
- 모든 인터랙티브 요소는 키보드로 조작 가능해야 한다.
- 색상 대비, 포커스 표시를 확보한다.

---

## 코딩 컨벤션 & 작업 방식

- 함수형 React 컴포넌트, Hooks 사용.
- 언어 코드·기능 플래그를 하드코딩하지 말고 데이터/설정으로 분리.
- **Phase 단위로 구현하고, 각 단계가 동작하는 것을 확인한 뒤 다음으로 넘어간다.** 한 번에 전체를 생성하지 말 것.
- 외부 호출(API·파일 IO)은 실패 시 크래시 대신 에러 메시지를 반환하도록 방어적으로 작성.

### 구현 순서 (Phase)

1. **Phase 0** — 프로젝트 셋업: Vite+React+Tailwind, `/api` 함수 골격, `.env`·`.gitignore`·폴더 구조, GitHub repo 생성 + Vercel 연동(배포 섹션 참고).
2. **Phase 1** — 전처리 파이프라인: `scripts/`로 시범 MP4 → `data/{contentId}/` JSON 생성·검수(로컬 실행).
3. **Phase 2** — 뷰어 + 6대 기능: 위 표 순서(①→②→⑤→③→⑥→④)대로 하나씩 구현·확인.
4. **Phase 3** — 접근성·보안 마무리: `axe-core` 검사, 키 서버사이드 검증, 관리자 검수 화면. (axe-core 생략 결정 — 「남은 과업」 2번 참고)
5. **Phase 4** — 배포: Vercel 환경 변수 등록, `main` 푸시로 자동 배포 확인, 프리뷰 URL 점검.

### 현재 구현 현황

콘텐츠는 영상 3개(sample-01 1강 · sample-02 2강 · sample-03 3강), 언어는 10개 전부.

| 기능 | 상태 | 비고 |
|---|---|---|
| ① 이중자막 | ✅ 완료 | 3콘텐츠 × 10개 언어(995 세그먼트) |
| ② 텍스트·UI 번역 | ✅ 완료 | ui-strings 26키 × 10개 언어, 콘텐츠 메타 10개 언어 |
| ② 이미지 오버레이 번역 | ✅ 완료 | 썸네일 bbox 오버레이(리스트·상세·관련영상) |
| ③ 읽어주기 | ✅ 완료 | OpenAI TTS + audioBus(한 번에 한 목소리) |
| ⑤ 어휘 즉시 풀이 | ✅ 완료 | 콘텐츠당 40개 × 10개 언어 |
| ⑥ 더빙 | ✅ 완료 | 3콘텐츠 × 9개 언어 = 8,955개 mp3(ko는 원음) |
| ④ 의미 기반 검색 | ✅ 완료 | `/api/search` 임베딩 + 메모리 코사인, 교차언어 검증. 결과 카드에 유사도(%·소수 2자리)와 매칭 대상 표기 |
| 리스트 페이지 | ✅ 완료 | 히어로·카테고리·카드 그리드 + 상세 라우팅 |
| 언어 10개 확장 | ✅ 완료 | 자막·어휘·UI·이미지·더빙·콘텐츠 메타 전부 |
| 온보딩 안내 모달 | ✅ 완료 | 첫 방문 자동 오픈 + '?' 버튼 수동, localStorage 지속, 포커스 트랩·Esc 닫기 |

**남은 과업**

진행 예정

1. AI 처리 파이프라인 시각화 — 기존 산출물(`subtitles.json`·`vocabulary.json`·`dub-{lang}.json`·`search-index.json`)을 읽어 그리는 정적 현황판 + ④ 의미 검색 원리 시각화. 상세는 [추가 과업 1](#1-ai-처리-파이프라인-시각화)
2. Phase 3 접근성·보안 마무리 — `axe-core` 도입·검사, 키 서버사이드 검증, 관리자 검수 화면. PoC·시연 단계에서는 axe-core 자동 검사를 생략한다. 지금까지 각 기능 구현 시 개별적으로 접근성(aria-*, 포커스 관리, 키보드 조작, 색상 외 신호 등)을 챙겨왔으므로 최소한의 접근성은 확보된 상태. 자동 검사 도구는 UI가 계속 바뀌는 개발 단계에 붙이면 검증 비용만 늘어나므로, 실제 서비스로 전환하는 시점에 마지막 단계로 미룬다. (2026-08-11 결정)
3. 온보딩 안내 모달 — ✅ 완료(「현재 구현 현황」 표 참고). 명세는 [추가 과업 2](#2-온보딩-안내)

선택(낮은 우선순위)

4. 모바일 'AI 도구' 시트 — 세부 컨트롤(자막 크기·읽기 속도·원음 볼륨)

스코프 아웃

5. 텍스트 콘텐츠 타입(`type: "text"`) — 스키마만 있고 미구현. 4대 원칙의 텍스트 축은 **영상 콘텐츠 메타데이터(제목·설명·해시태그·강사명)의 10개 언어 번역**으로 갈음한다. ② 텍스트 번역 기능은 이 메타데이터 번역으로 이미 충분히 검증돼, 별도 텍스트 콘텐츠 타입은 구현하지 않기로 결정(2026-08-11 결정)
6. 검색 인덱스 세그먼트 단위 확장 — 현재 인덱스는 콘텐츠당 **한국어 제목+설명+해시태그 벡터 1개**(콘텐츠 단위 매칭). 자막 세그먼트별·언어별 벡터로 확장하면 "영상 속 어느 대목/어느 언어와 매칭됐는지"까지 표시 가능. PoC 시연에는 현재 수준으로 충분해 스코프 아웃(2026-07-27 결정)

---

## 추가 과업 (본 기능 완성 후)

6대 기능이 안정화된 뒤 얹는 부가 과업. 새 데이터 파이프라인을 만들지 않고 **기존 산출물·컴포넌트를 재사용**한다.

### 1. AI 처리 파이프라인 시각화

실제 산출물(`subtitles.json`·`vocabulary.json`·`dub-{lang}.json` 등)을 읽어, 콘텐츠가 어떤 단계로 처리됐는지 보여주는 **정적 현황판**. 실제 업로드 처리가 아니라 **기존 데이터 기반 표시**다.

- 각 단계에 **사용 기술 + 결과 수치**를 함께 표시한다:
  1. 오디오 추출 — ffmpeg
  2. 음성 인식 — OpenAI Whisper (N세그먼트)
  3. 자막 교정 — Claude
  4. 다국어 번역 — Claude (N개 언어)
  5. 어휘 추출 — Claude (N개)
  6. 더빙 음성 — OpenAI TTS (언어별)
- 수치는 산출물에서 계산해 채운다: 세그먼트 수 = `subtitles.json`의 `segments.length`, 언어 수 = `languages.length`, 어휘 수 = `vocabulary.json`의 `terms.length`, 더빙은 존재하는 `dub-{lang}.json` 언어별 `count`.
- **목적**: 시스템 구조를 한눈에 파악·설명 가능하게. 심사·시연에서 "이 앱이 무엇을 어떻게 처리했는지"를 즉시 보여준다.

또한 전처리 흐름(오디오 추출→STT→교정→번역→어휘→더빙)에 더해, **④ 의미 기반 검색이 어떻게 작동하는지**도 같은 현황판에서 시각화한다. 핵심 메시지는 **"단어 일치가 아니라 의미(뜻)로 찾는다"**.

- **전처리(미리 계산)**: 콘텐츠(제목·설명) → **임베딩**(문장을 숫자 배열로 변환, `text-embedding-3-small`) → `data/search-index.json`에 콘텐츠별 벡터로 저장.
  - `search-index.json`의 `items[].embedding`(숫자 배열)이 실제 산출물 — "제목·설명이 이런 숫자 좌표가 됐다"를 보여준다.
- **런타임(질의 시)**: 사용자 질의 → **임베딩**(같은 모델, `/api/search` 경유) → 저장된 콘텐츠 벡터들과 **코사인 유사도(거리)** 비교 → **가장 가까운 콘텐츠 반환**.
- 시각화 방식(택1): 질의 벡터와 콘텐츠 벡터들의 유사도 점수를 막대/거리로 나열하거나, 벡터를 2D로 눌러 "질의 점 주변에 가까운 콘텐츠가 모이는" 산점도로 표시. 예시 질의(모국어) → 단어가 안 겹쳐도 뜻이 통하는 한국어 콘텐츠가 상위에 오는 모습을 보여준다.
- **목적**: "왜 단어가 달라도 찾아지는가"를 한눈에 보여주고, 본인도 남에게 임베딩·유사도 원리를 설명할 수 있게.

### 2. 온보딩 안내

첫 방문 시 자동으로 뜨는 **안내 모달** + 상단 **"?" 도움말 버튼**으로 언제든 다시 열기.

- **모달 내용은 하나**, 여는 트리거만 둘: 자동(첫 방문) · 수동("?" 버튼).
  - 첫 방문 판정은 `localStorage` 플래그로. 이후 방문에는 자동으로 뜨지 않는다.
- 주요 기능 **3~4개를 카드로 소개**: 모국어 번역 · 읽어주기/더빙 · 어휘 풀이 · 의미 검색.
- 안내 문구도 **`t()`로 다국어 지원** — 한국어 하드코딩 금지. 카드 문구 키는 `ui-strings.json`에 추가한다.
