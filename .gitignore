# 의존성
node_modules

# 빌드 산출물
dist

# 환경 변수 (API 키 — 커밋 금지)
.env
.env.local
.env.*.local

# 원본 영상 (954MB MP4 — STT 입력용, 로컬 전용)
source/

# 로그·OS
*.log
.DS_Store
Thumbs.db

# 전처리 중간 산출물 (오디오 추출·청크)
*.tmp.mp3

.vercel
